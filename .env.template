# ============================================================================
# PDF Signer - Configuración de Variables de Entorno
# ============================================================================
# Copia este archivo a .env y configura tus valores personales
# Comando: cp .env.template .env (Mac/Linux) o copy .env.template .env (Windows)

# --- CONFIGURACIÓN REQUERIDA ---

# Directorio de entrada con los PDFs a firmar
PDF_INPUT_DIR=./docs

# Directorio de salida para los PDFs firmados
PDF_OUTPUT_DIR=./signed

# Ruta al certificado PFX/P12
PDF_CERT_PATH=./key/cert.pfx

# Contraseña del certificado
PDF_CERT_PASSWORD=your_password_here

# --- CONFIGURACIÓN DE FIRMA VISIBLE ---

# Habilitar firma visible (true/false)
PDF_VISIBLE=true

# Página de firma (1 = primera, -1 = última)
PDF_SIG_PAGE=1

# Coordenadas y Dimensiones (Valores del usuario)
# Configuración por defecto: x=10, y=122, w=27, h=13
PDF_SIG_RECT_X=10
PDF_SIG_RECT_Y=122
PDF_SIG_WIDTH=27
PDF_SIG_HEIGHT=13

# Texto de la firma (layer2Text)
# Se permiten saltos de línea con \n
# Variables disponibles: $$SUBJECTCN$$, $$ISSUERCN$$, $$SIGNDATE=pattern$$, $$LOCATION$$, $$REASON$$
PDF_SIG_TEXT="Firmado por $$SUBJECTCN$$ el día $$SIGNDATE=dd/MM/yyyy$$ con un certificado emitido por $$ISSUERCN$$"

# --- CONFIGURACIÓN AVANZADA (Estilo y Rúbrica) ---

# Color del texto (black, blue, red, etc.)
PDF_SIG_COLOR=black

# Ruta a imagen de rúbrica (JPEG/PNG) - Opcional
# Si se deja vacío no se usa imagen.
PDF_SIG_IMAGE_PATH=

# Alias del certificado (opcional, se auto-detecta si se omite)
PDF_ALIAS=

# --- METADATA (Opcional) ---
# Ubicación para la firma
# Ejemplo: Madrid, España
PDF_LOCATION=

# Razón de la firma
# Ejemplo: Firma de documentos oficiales
PDF_REASON=

# Añadir timestamp a la firma (true/false)
PDF_TIMESTAMP=false
