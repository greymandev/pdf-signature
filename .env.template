# ============================================================================
# PDF Signer - Configuración de Variables de Entorno (VERSIÓN MEJORADA)
# ============================================================================
# Copia este archivo a .env y configura tus valores personales
# Comando: cp .env.template .env (Mac/Linux) o copy .env.template .env (Windows)

# --- CONFIGURACIÓN REQUERIDA ---

# Directorio de entrada con los PDFs a firmar
PDF_INPUT_DIR=./docs

# Directorio de salida para los PDFs firmados
PDF_OUTPUT_DIR=./signed

# Ruta al certificado PFX/P12
PDF_CERT_PATH=./key/cert.pfx

# Contraseña del certificado
PDF_CERT_PASSWORD=your_password_here

# --- CONFIGURACIÓN DE FIRMA VISIBLE ---

# Habilitar firma visible (true/false)
PDF_VISIBLE=true

# Perfil de firma (opcional) - Ver signature_profiles.json
# Si se especifica, usa las coordenadas del perfil en lugar de las variables de abajo
# Opciones: default, bottom_right, top_right, last_page, with_image
PDF_PROFILE=default

# Página de firma (1 = primera página, -1 = última página, 2 = segunda página, etc.)
PDF_SIG_PAGE=1

# Coordenadas y Dimensiones de la firma visible
# IMPORTANTE: Las coordenadas en PDF se miden desde la esquina inferior izquierda
# Unidades: puntos (1 pulgada = 72 puntos)
# Ejemplo: A4 es aproximadamente 595 x 842 puntos
#
# Coordenada X (desde la izquierda)
PDF_SIG_RECT_X=10

# Coordenada Y (desde abajo)
PDF_SIG_RECT_Y=122

# Ancho de la caja de firma
PDF_SIG_WIDTH=27

# Alto de la caja de firma
PDF_SIG_HEIGHT=13

# Texto de la firma (layer2Text)
# Variables disponibles:
#   $$SUBJECTCN$$ - Nombre del firmante
#   $$ISSUERCN$$ - Nombre de la entidad emisora del certificado
#   $$SIGNDATE=FORMATO$$ - Fecha de firma (ej: dd/MM/yyyy, yyyy-MM-dd HH:mm:ss)
#   $$LOCATION$$ - Ubicación de la firma
#   $$REASON$$ - Razón de la firma
#
# Se permiten saltos de línea con \n
PDF_SIG_TEXT="Firmado por $$SUBJECTCN$$ el día $$SIGNDATE=dd/MM/yyyy$$ con un certificado emitido por $$ISSUERCN$$"

# --- CONFIGURACIÓN AVANZADA (Estilo y Rúbrica) ---

# Color del texto (black, blue, red, green, etc.)
PDF_SIG_COLOR=black

# Familia de fuente (opcional)
# PDF_SIG_FONT_FAMILY=Helvetica

# Tamaño de fuente (opcional)
# PDF_SIG_FONT_SIZE=10

# Ruta a imagen de rúbrica (JPEG/PNG) - Opcional
# Si se deja vacío no se usa imagen.
# NOTA: En AutoFirma 1.7.2 debe ser codificada en base64 (comando muy largo)
# En AutoFirma 1.8+ se puede usar la ruta directa al archivo
PDF_SIG_IMAGE_PATH=

# Versión de AutoFirma (para determinar compatibilidad de imágenes)
# Opciones: 1.7, 1.8, 1.9
AUTOFIRMA_VERSION=1.7

# Modo de renderizado de la firma visible
# 1 = Descripción + Gráfico (imagen/rúbrica)
# 2 = Solo gráfico
# 3 = Solo descripción (texto)
PDF_SIG_RENDERING_MODE=1

# Alias del certificado (opcional, se auto-detecta si se omite)
PDF_ALIAS=

# --- METADATA DE LA FIRMA (Opcional) ---

# Ubicación para la firma (aparece en los metadatos del PDF)
# Ejemplo: Madrid, España
PDF_LOCATION=

# Razón de la firma (aparece en los metadatos del PDF)
# Ejemplo: Firma de documentos oficiales
PDF_REASON=

# Añadir sello de tiempo a la firma (true/false)
# Requiere conexión a Internet para contactar con la TSA
PDF_TIMESTAMP=false

# --- CONFIGURACIÓN DE DEPURACIÓN ---

# Modo verbose para más información de depuración (true/false)
DEBUG=false
